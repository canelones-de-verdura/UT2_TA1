function repeatString(texto, repeticiones) {
    for (i = 0; i < repeticiones; ++i) {
        console.log(texto);
    }
}

const texto = "tirabuzones con tuco";
const veces = 10;
repeatString(texto, veces);
